---
name: hengchang-order-manager
description: 恒昌橡塑订单管理自动化。识别图片/Excel订单或发货单，写入腾讯文档表格。支持生成对账单。
version: 2.4.0
agent_created: true
---

# 恒昌橡塑订单管理

**腾讯文档**: file_id=`VGTBMcHrbUbo`, 3个sheet:

| Sheet | sheet_id | 用途 |
|-------|----------|------|
| 客户订单总表 | `BB08J2` | 17列: A客户名称 B订单号 C下单日期 D交货期 E序号 F物料编码 G物料名称 H规格型号 I数量 J单位 K不含税单价 L税率 M含税单价 N含税金额 O已发数量(公式) P未发数量(公式) Q发货记录(公式) |
| 发货总单 | `Gikgrt` | 11列: A录入日期 B送货日期 C送货单号 D代工厂 E序号 F产品名称 G规格型号(存物料编码) H订单号 I单位 J数量 K备注 |
| 对账单 | `Mzfnaw` | 9列: A序号 B产品名称 C型号 D数量 E含税价 F金额 G送货日期 H订单号 I备注 |

## 列映射（发货总单 → 订单总表）

| 发货总单 | ↔ | 订单总表 |
|---------|---|---------|
| G列 规格型号（=物料编码，如3307-0187） | = | F列 物料编码 |
| F列 产品名称 | = | G列 物料名称 |
| H列 订单号 | = | B列 订单号 |

## 核心规则

1. **只追加，不动已有行**。读表找末行，紧接其后追加新行。
2. **序号从1开始**。每张订单/发货单的E列序号各自独立，不接续前面的序号。
3. **新增行标黄色** `FFFFFFB3`，用户复核后取消。
4. **确认即写**：用户说"确认/无误/录入" → 直接读表找末行 → 写入 → 标黄 → 报结果。不二次追问。

## 场景1: 录入客户订单（图片/Excel → BB08J2）

**任务**：把图片/Excel里每行产品写到BB08J2末尾，每个产品一行。14列手动值（A~N）照抄识别结果，3列公式（O/P/Q）自动关联发货数据。同一订单的头几列（客户名、订单号、下单日期、交货期）每个产品行都复制一遍。

**公式模板**（N=实际行号，0-based；公式中写N+1作为Excel行号）：

O列: `=SUMIFS(发货总单!J:J,发货总单!H:H,B{N+1},发货总单!F:F,G{N+1},发货总单!G:G,F{N+1})`
P列: `=I{N+1}-O{N+1}`
Q列: `=TEXTJOIN("；",TRUE,IF((发货总单!$H$2:$H$500=B{N+1})*(发货总单!$F$2:$F$500=G{N+1})*(发货总单!$G$2:$G$500=F{N+1}),发货总单!$C$2:$C$500&"("&发货总单!$B$2:$B$500&","&发货总单!$J$2:$J$500&"个)",""))`

写入用 `mcp__tencent-docs__sheet.set_range_value`，写完后用 `set_cell_style` 标黄。

## 场景2: 录入发货单（图片/Excel → Gikgrt）

**任务**：把图片/Excel里每行产品写到Gikgrt末尾，每个产品一行，11列全写（A录入日期=当天 B送货日期 C送货单号 D代工厂 E序号 F产品名称 G编码 H订单号 I单位 J数量 K备注）。绝对不要碰BB08J2——它的O/P/Q公式会自动从Gikgrt拉数据。

写入用 `set_range_value`，写完后用 `set_cell_style` 标黄。

## 场景3: 生成对账单（指定日期范围 → Mzfnaw）

**任务**：从Gikgrt筛出送货日期(B列)在指定范围内的所有行，每条原样转写到Mzfnaw末尾。对账单9列填法：A序号(重新从1编号) B产品名称(=Gikgrt.F) C型号(=Gikgrt.G，即物料编码) D数量(=Gikgrt.J) E含税价(公式) F金额(公式) G送货日期(=Gikgrt.B) H订单号(=Gikgrt.H) I备注(=Gikgrt.K)。不要清空旧数据。

**公式模板**（N=实际行号，0-based）：

E列(含税价): `=SUMIFS(客户订单总表!M:M,客户订单总表!B:B,H{N+1},客户订单总表!G:G,B{N+1},客户订单总表!F:F,C{N+1})`
F列(金额): `=D{N+1}*E{N+1}`

写入后标黄，报合计金额。

## 踩坑

- AI用Read直接读图 >> OCR
- 公式中第三个条件用F列(物料编码)，不是H列(规格型号描述)
- 发货总单G列存的是物料编码，订单总表F列也是物料编码 — 这样才能匹配
- 公式行号：0-based的N，公式中写N+1
- 税率写小数0.13
- 日期格式`YYYY/M/D`

## 客户

| 简称 | 全称 |
|------|------|
| 赛特莱特 | 赛特莱特（佛山）塑胶制品有限公司 |
